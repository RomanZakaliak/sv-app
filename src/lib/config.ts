import { dev } from "$app/environment";

export const title = "A favorite poet!";
export const description = "A SvelteKit blog about poets!"
export const url = dev ? "http://localhost:5173/" : "./{placeholder_change_later}";